version: '3.8'

services:
  simpleWebServer:
    image: alpine:3.19
    command: ["/bin/sh", "-c", "while true; do echo 'HTTP/1.1 200 OK\n\nHello World!' | nc -l -p 80; done"]
    ports:
      - "80"
