
TODO: Pet Clinic REST API design
Design REST service related with Pet Clinic, where pets have name and age.
Add operation with pets batch update (for example add new field - owner)

GET     clinic\{id}
DELETE  clinic\{id}
POST    clinic
            params: { name: ..., age : ... }
PUT     clinic\{id}
            params: { age: ... }
PATCH   clinic
            params: { ids: ..., owner: ... }

TODO: Реализовать, используя только JDK (без сервлетов и фреймворков), одну простейшую операцию REST-сервиса
Подсказка: стоит использовать сокеты
